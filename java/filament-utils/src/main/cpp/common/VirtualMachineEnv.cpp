#include "VirtualMachineEnv.h"

namespace filament {

// This is typically called from JNI_OnLoad
// For now we will rely on the header-only getEnvironment but we can add more robust logic here if needed.
// However, JNI_OnLoad is the best place to set the JVM.

} // namespace filament
